# todo

## 오퍼코드 CSV → 선물용 실물/디지털 배포

- [x] CSV 구조 파악 (실수 100, id 6759643878, 1회용 코드 500개)
- [x] 순수 JS QR 인코더 작성 (`tools/qr.js`, 외부 의존성 없음)
- [x] macOS Vision으로 QR 디코딩 검증 — 500개 × ECC 4단계 = 2,000건 전량 통과
- [x] `tools/giftsheet.html` — CSV 드롭 → A4 인쇄 시트 (명함 10 / 와이드 8 / A6 4 / 라벨 24)
- [x] 링크에서 Apple ID 자동 추출 → 앱 이름·아이콘 자동 조회
- [x] 개별 전송 모드 (링크 문구 우선 + 카드 PNG)
- [x] 배포 대장 + CSV 내보내기
- [x] 헤드리스 브라우저로 동작 검증 (현재 28개 항목)
- [x] 인쇄 PDF를 300dpi로 래스터화 → QR 10개 전부 정상 디코딩 확인

## 기프트 카드 디자인 (완료)

- [x] 실물 규격 리서치 → CR80 85.6×54mm, 모서리 3mm, 안전여백 3mm 적용
- [x] 액면(혜택) 히어로 · "기프트 카드" 커커 · 약관 한 줄 · 유효기간 필드
- [x] 카드 질감 — 광택 스윕, 헤어라인 프레임, 라운드 다이컷 가이드
- [x] 앞/뒷면 양면 모드 (뒷면 = 발행처 + 사용 방법 + 코드 + 약관 + QR)
- [x] 긴 쪽 넘기기 듀플렉스용 뒷면 좌우 미러링
- [x] 디지털 카드 — 그림자 얹은 실물 카드 형태로 재작업
- [x] 타이포그래피를 카드 높이에 비례 스케일 (--u)

## 앱 지정 입력 (완료)

- [x] Apple ID 칸이 리딤 링크 / 앱 링크 / itms-apps / 숫자를 모두 인식
- [x] 링크는 붙여넣는 즉시 조회, 숫자는 Enter·blur에서 조회 (반쪽 번호 헛조회 방지)
- [x] 한국 스토어에 없으면 기본 스토어로 폴백
- [x] 조회 실패 안내가 남아 있던 표시 갱신 버그 수정
- [x] 앱 본체 `RedeemLinkBuilder.appleID(from:)`도 `id=` 형태 인식하도록 수정 (11개 케이스 테스트 + 빌드 통과)

## 링크 하나로 전달 방식 자동 결정 (완료)

- [x] `RedeemLinkBuilder.parse()` — 링크에서 id · ctx · code · pt · ct 전부 추출 (예전엔 id만)
- [x] `inferredKind()` — ctx가 있으면 그대로, 없으면 유료→프로모 / 무료+코드→오퍼 / 무료+코드없음→링크
- [x] 경로 슬러그(`/app/id999.../id6759643878`)보다 쿼리 `id=`를 우선해 오인식 방지
- [x] 사용자가 방식을 직접 고르면 이후 조회가 덮어쓰지 않음 (`kindChosenManually`) — 단 새 링크의 `ctx=`는 우선
- [x] "링크에서 … 자동 설정" 안내를 전달 방식 섹션에 표시
- [x] `giftsheet.html`도 `ctx=` 인식 (예전엔 `offercodes` 하드코딩)
- [x] giftsheet도 글자 블록 크기 조절 개방 — 배율이 폰트·자간·줄간격·상자 여백에 곱해지고,
      인쇄 카드는 블록별 `--t` 커스텀 속성으로 같은 배율을 받음 (QR 캡션만 카드 기본값 유지)
- [x] 파서 22개 케이스 + giftsheet 8개 케이스 통과, 앱 빌드 성공

## 드래그 배치 편집기 (완료)

- [x] 미리보기에서 6개 블록(앱 이름·액면·메시지·코드·약관·QR) 드래그 이동
- [x] 여백선·중앙·다른 블록 모서리 스냅 + 가이드선, Alt로 스냅 해제
- [x] 방향키 미세 조정 (Shift 10px), Esc 선택 해제, 카드 밖 이탈 방지
- [x] 배치를 비율 좌표로 저장 → 인쇄 카드에 동일 적용, localStorage 유지, 초기화 버튼
- [x] 프리뷰/인쇄 글자 크기 통일 (1000px = 85.6mm) — 두 렌더러 어긋남 제거
- [x] 내보낸 PNG에 편집 UI가 섞이지 않도록 분리 (픽셀 검사로 확인)
- [x] 블록 래퍼 클래스 네임스페이스 `b-` — `.note`/`.code`/`.fine` 스타일 충돌 수정

## 로고·QR 위치/크기 조절 (완료)

- [x] 앱 로고를 `brand`에서 떼어내 독립 블록으로 — 이제 이름과 따로 움직임
- [x] 구버전 저장 배치 자동 마이그레이션 (아이콘 +92px / 이름 +20px로 풀어냄) — 카드 모양 그대로
- [x] 로고·QR 크기 조절 — 선택 상자 오른쪽 아래 손잡이 드래그, 40~250%, 5% 스냅 (Alt 무단계)
- [x] `+`/`−` 키로 5%, `Shift`와 함께 20% — 선택 이름표에 현재 배율 % 표시
- [x] 키우다 카드 밖으로 나가면 안쪽으로 되당김
- [x] 인쇄 카드도 같은 배율 적용 (QR은 폭 %, 로고는 mm) — 프리뷰와 인쇄물 일치
- [x] 편집기 로직 23개 케이스 통과 (마이그레이션·클램프·손잡이 히트박스)
- [x] `moveBlock`이 layout 항목을 통째로 갈아끼워 배율을 날리던 버그 수정 — 크기 조절 후
      살짝만 옮겨도 100%로 돌아가던 문제 (헤드리스 브라우저 검증에서 발견)
- [x] 헤드리스 Chrome으로 실제 조작 검증 — 블록 선택·+/− 키·손잡이 드래그·배치 초기화,
      인쇄 카드에 QR 25.68%(21.4×1.2) / 로고 5.064mm(6.33×0.8) 반영 확인

## 앱 아이콘 (완료)

- [x] `tools/appicon.swift` — CoreGraphics로 선물 상자 아이콘 생성 (리본 십자 + 나비 매듭)
- [x] 카드와 같은 `sunrise` 팔레트 사용 — 아이콘과 결과물이 같은 제품으로 읽히게
- [x] macOS 규격대로 1024 캔버스에 824 타일 + 그림자, 16~1024px 7종 출력
- [x] `Contents.json` 10개 슬롯 연결 → 빌드 시 `AppIcon.icns` 생성 확인

## 글자 크기 키우기 (완료)

- [x] 카드 글자 전체를 `TYPE_SCALE` 1.18배로 — 코드 7pt→8.6pt, 약관 3.9pt→4.6pt
- [x] 프리뷰(캔버스 px)와 인쇄 카드(CSS `--t`)가 같은 값을 쓰도록 통일 — 13곳 일괄 적용
- [x] 커진 글자에 맞춰 `DEFAULT_LAYOUT` 세로 예산 재계산 (519px 중 508px 사용, 겹침 없음)
- [x] 배치 저장본에 `__v` 버전 도입 — 옛 좌표는 버리고 기본값 복귀 + 안내문 표시
- [x] 도구 UI 글자도 13곳 키움 (본문 15→16, 안내 13→14, 라벨 12→13 등)
- [x] 버전 게이트·세로 예산 13개 케이스 통과, 헤드리스로 캔버스·인쇄 카드 육안 확인
- [x] 편집기 13개 항목 재검증 — 글자 확대 후에도 선택·크기 조절·초기화 정상

## 맥 앱 — 카드 요소 드래그 배치 (완료)

- [x] `CardLayout` 모델 — 8개 블록(머리말·배지·아이콘·앱이름·메시지·사람·코드·QR)의
      위치·배율을 캔버스 비율로 저장, UserDefaults에 버전과 함께 보관
- [x] `GiftCardView`를 VStack/HStack 흐름에서 절대 배치로 전환 — 요소가 서로 밀지 않음
- [x] `EditableCardPreview` — 클릭 선택, 드래그 이동, 여백선·중앙·다른 블록 모서리 스냅 +
      안내선, Option으로 스냅 해제
- [x] 아이콘·QR 크기 조절 — 모서리 손잡이 드래그(5% 단위, Option 무단계), +/− 키(Shift 20%)
- [x] 글자 블록도 크기 조절 — 배율이 폰트 크기에 곱해지고, 자간·줄간격·안쪽 여백·줄바꿈
      폭까지 같이 커져서 덩어리째 커짐 (처음엔 글자를 일부러 제외했는데 요청으로 열음)
- [x] 방향키 1pt / Shift 10pt 미세 조정, Esc 선택 해제, 배치 초기화 버튼
- [x] 선택 표시는 뷰 좌표로 그려서 카드를 축소해도 손잡이 크기가 그대로
- [x] 드래그·스냅·클램프 계산을 View에서 모델로 분리 — 뷰 없이 검증 가능하게
- [x] PNG 내보내기와 배치 내보내기도 같은 배치 사용 (HTML 선물 페이지는 고정 레이아웃 유지)
- [x] 맥 앱 카드 글자도 `typeScale` 1.18배 — giftsheet와 같은 기준
- [x] 프리뷰에서 카드가 잘리던 버그 수정 — `scaleEffect(anchor: .topLeading)`을 쓰면서
      바깥 프레임 정렬을 기본(가운데)으로 둬서, 카드 레이아웃 상자가 가운데 놓인 채
      자기 왼쪽 위를 기준으로 축소돼 위/왼쪽 바깥으로 밀려나 있었음
- [x] 27개 케이스 통과(히트테스트·스냅·클램프·저장 왕복·기본 배치 겹침 검사 +
      두 가지 창 비율에서 카드가 프레임을 다 채우는지 픽셀 검사)
- [x] 픽셀 검사가 실제로 그 버그를 잡는지 정렬 되돌린 사본으로 역검증
- [x] 헤드리스 렌더로 기본/변형 배치 육안 확인, 앱 실행 크래시 없음 확인

## 선물 페이지 (완료)

- [x] `web/gift.html` — 값을 URL 프래그먼트로 받아 렌더하는 정적 페이지 하나
- [x] 닫힌 상태 → 포장 풀리는 애니메이션(리본 이탈 · 나비 팝 · 타일 버스트) → 카드 공개 → 받기 버튼
- [x] 코드·받는 사람 이름을 쿼리스트링이 아닌 `#` 뒤에 실어 서버 로그·리퍼러에 안 남김
- [x] `그냥 바로 받기` 탈출구 + `prefers-reduced-motion` 자동 건너뛰기
- [x] App Store에서 돌아오면 "받으셨나요?" + 보낸 사람 이름 + `앱 열기` (인계 지점에서 주는 사람 유지)
- [x] 아이콘 로드 실패 시 첫 글자 모노그램으로 대체, 프래그먼트가 깨져도 데모로 폴백
- [x] 앱 쪽 `GiftLinkBuilder` — 드래프트를 짧은 키 JSON → base64url로 인코딩
- [x] 선물 링크를 쓸 땐 문구에서 앱 이름·코드를 뺌 (페이지가 풀 것을 미리 흘리지 않도록)
- [x] 컴포저에 선물 페이지 주소 입력 + 링크 복사 + 미리보기
- [x] 왕복 23개 케이스 통과(한글 메시지·만료일·테마색·코드 왕복, 티저 문구 검증), URL 607자
- [x] 실제 링크로 페이지 4단계(닫힘·여는 중·공개·복귀) 렌더 확인
- [x] 한 번의 `공유` 동작 — 시스템 공유 시트에 문구(링크 포함) + 카드 이미지를 함께 넘김
- [x] GitHub Pages 배포 — `gh-pages` 브랜치(페이지 파일만) → https://m1zz.github.io/GiftWrap/gift.html
- [x] 주소를 설정이 아니라 상수(`GiftLinkBuilder.pageURL`)로 — 이미 아는 값을 되묻던
      입력 칸과 "주소 없을 때" 분기(문구 3종·메뉴 비활성)를 전부 제거
- [x] 배포본이 원본과 바이트 동일한지, 실제 https 주소에서 링크가 풀리는지 확인
- [x] 링크가 QR·코드 표시 토글과 **블록 배치 전체**(위치·배율 8개)를 실어 나름
- [x] 페이지가 자기 레이아웃을 그리던 것을 폐기 — 같은 1000×630 좌표계를 `cqw`로 재현해
      컴포저에서 잡은 배치 그대로 렌더 (옮기고 키운 것이 폰에서도 그대로)
- [x] QR을 페이지에서 실제로 그림 — `tools/qr.js`를 인라인해 단일 파일 유지
- [x] 이미지 URL(`img`) 필드 예비 — 넣으면 렌더 대신 그 이미지를 씀
- [x] 배치 왕복 8개 케이스 통과 + 기본/변형 배치 렌더 비교 + 실제 https 주소 확인
- [x] 웹에서 코드 칩이 QR 밑으로 들어가 잘리던 문제 — 앱은 SF Rounded/SF Mono 폭으로 배치를
      잡는데 브라우저엔 그 글꼴이 없어 같은 글자가 더 넓게 렌더됨. 앱이 잰 블록 상자(w·h)를
      링크에 실어 페이지가 그 자리에 맞추고, 그래도 넘치면 카드 안으로 당김
- [x] 겹침·이탈 자동 검사 — 측정값 있으면 CLEAN, 없으면 `overlap:code/qr` 재현
- [x] 여러 장 만들기도 선물 링크로 — 100장이 전부 포장된 링크로 나가고, 매니페스트 CSV에
      `gift_link`와 `redeem_link`를 함께 기록
- [x] 보낸 기록이 실제로 보낸 링크(선물 링크)를 저장 — 나중에 다시 보낼 수 있게
- [x] `giftsheet.html`도 선물 링크 생성 (배치는 안 실림 — 인쇄 카드는 블록 구성이 달라
      대응 관계가 없어서, 페이지는 기본 배치로 같은 내용을 보여줌)
- [ ] 카드 PNG 자체를 페이지로 보내는 건 보류 — 링크에 넣기엔 크고, 호스팅하면 받는 사람
      이름·메시지가 공개 URL에 남음. Worker + 비공개 저장소가 생기면 `img`로 연결

## 링크 복사 버튼 명시 (완료)

- [x] `복사`·`선물 링크 복사`처럼 무엇이 복사되는지 모호하던 이름을
      `선물 페이지 링크 복사` / `프로모션 링크만 복사`로 바꿈
- [x] 두 버튼을 `링크 복사` 제목 아래 강조 박스로 묶고, 각 버튼에 아이콘 +
      한 줄 설명("카드가 먼저 열리는 선물 페이지" / "App Store로 바로 가는 원본 링크")
- [x] 주력인 선물 페이지 쪽만 채운 버튼으로 두어 기본 선택이 눈에 들어오게
- [x] 복사 후 상태 문구도 어떤 링크였는지 말하도록 수정

## 선물 링크 줄이기 (완료)

- [x] 링크 1166자 → 814자 (−30%). 블록 배치(`l`, 843바이트 중 263)를 빼서
- [x] 배치는 페이지 기본 레이아웃으로 — 겹침은 기존 `separate()`가 막음
- [x] 아이콘 URL(`i`)은 다시 실음 — 앱스토어 링크처럼 변수로 명시.
      한 번 589자까지 줄였다가(페이지가 Apple ID로 iTunes lookup) 되돌림:
      170바이트를 아끼려고 받는 사람 쪽에 네트워크 왕복과 모노그램 깜빡임을 추가할
      이유가 없음. lookup 경로는 `i`가 없을 때의 폴백으로만 남김
- [x] 앱에서 죽은 배선 제거 (`ComposerModel.blockSizes`, `EditableCardPreview.onMeasure`)
- [x] `giftsheet.html`도 아이콘 URL 안 실음
- [x] 옛 링크(`i`·`l` 있는 것)는 그대로 열림 — 페이지가 있으면 쓰고 없으면 대체
- [ ] 남은 선택지: 구조 정리(u/p를 id로) −100자, deflate 압축 −20%,
      짧은 도메인 −20자, Worker 단축링크 (프라이버시 대가 있음)

## 블록 겹침 경고 (완료)

- [x] 원인 — 저장된 배치에서 앱 아이콘이 1.45배(152→220pt). 아이콘 아래끝 350pt,
      메시지 시작 320pt로 30pt 겹치고 메시지가 나중에 그려져 아이콘을 덮음
- [x] `CardLayout.collisions(sizes:slack:)` — 겹치는 블록 쌍을 찾음 (3pt는 봐줌)
- [x] 편집기에서 겹친 블록에 주황 점선 + 옅은 채움, 카드 아래에 어떤 둘이 겹쳤는지 알림
- [x] `겹친 것만 되돌리기` — 문제 블록만 기본 위치로, 나머지 배치는 보존
      (`배치 초기화`는 전부 버려서 과함)
- [ ] 배너 실물 확인 못 함 — osascript 보조 접근이 거부되어 GUI 자동화 중단.
      메시지를 넣은 상태로 눈으로 확인 필요

## `#` → `?d=` 전환 (완료)

- [x] 증상 — 선물 페이지 링크를 채팅에 붙여넣으면 `#`에서 끊겨 앞부분만 링크가 되고
      `#eyJ…`는 일반 텍스트로 떨어짐. 프래그먼트가 선물 내용 전부라 받는 사람은 데모를 봄
- [x] 앱은 무죄 확인 — 클립보드에 684자 전체가 `https://`부터 온전히 들어감
- [x] payload를 `?d=`로 옮김 (앱 · `giftsheet.html` 양쪽). base64url 알파벳이
      쿼리 값에 그대로 legal해서 % 이스케이프 없이 읽힘
- [x] 페이지는 `?d=` 우선, 없으면 프래그먼트 — 이미 보낸 `#` 링크는 계속 열림
- [x] `<meta name="referrer" content="no-referrer">` — payload가 Apple·mzstatic
      리퍼러로 새는 것 차단. 노출 범위를 GitHub 접근 로그로 한정
- [x] 페이지의 실제 파싱 코드를 꺼내 4개 경우 통과 (?d= · # · 둘 다 · 빈 주소)
- 대가: 코드와 받는 사람 이름이 GitHub Pages 접근 로그에 남음.
  프래그먼트를 자르는 메신저에서는 프래그먼트에 든 비밀이 무조건 사라지므로,
  "안 끊기는 링크"와 "서버가 못 보는 링크"는 동시에 만족 불가

## 한/영 이중 언어 (완료)

- [x] 구조 결정 — UI 언어(운영자)와 카드 언어(받는 사람)를 **분리**. 한국인이 영어권 지인에게
      보내는 경우가 실제 용례라, 카드·선물 페이지·전송 문구는 보내는 사람이 따로 고름
- [x] `Localization.swift` — `AppLanguage(ko/en)` · `Loc{ko,en}` 값 타입 · UI용 `T` · 받는 사람용 `C`.
      `.strings` 대신 Swift 테이블: `Localizable.strings`는 실행 중 전환이 안 되고
      (번들이 launch 시점에 고정), 누락 번역이 런타임 폴백 대신 컴파일 에러가 됨
- [x] 앱 메뉴바에 `Language` 메뉴 — 재시작 없이 즉시 전환, UserDefaults에 유지
- [x] `GiftDraft.cardLanguage` + 편집기에 `카드 언어` 세그먼트 · 링크 payload에 `lg` 키
      (`l`은 옛 링크의 블록 배치라 재사용 불가)
- [x] `occasion` 기본값을 `"선물"` → `""`로 — 비어 있으면 카드 언어의 기본 머리말을 그림.
      리터럴로 박아두면 언어를 바꿔도 옛 언어가 남음
- [x] `web/gift.html` — `lg` 우선, 없으면 `navigator.language`. 우측 상단 KO/EN 토글로
      카드까지 다시 그림(포장 상태는 유지). 데모 선물도 언어 따라감
- [x] `tools/giftsheet.html` — `data-i18n*` 속성 + UI 토글(헤더) + 카드 언어 select(3번 섹션).
      카드 기본 문구(액면·메시지·약관)는 **손대지 않은 것만** 새 언어 기본값으로 옮김.
      두 선택 모두 localStorage에 유지
- [x] 앱 빌드 통과 · 두 페이지 브라우저 실동작 확인(전환·유지·인쇄 뒷면·전송 문구·콘솔 무오류)

### 이 작업에서 고친 곳

- [x] `gift.html`이 payload의 이름을 이스케이프 없이 `innerHTML`에 넣고 있었음 —
      `?d=`는 누구나 만들 수 있어 XSS가 됨. `esc()` 추가
- [x] `ShareAnchor`가 `NSHostingView`를 한 번만 만들고 `updateNSView`가 비어 있어서
      언어를 바꿔도 `공유`/`Share` 라벨이 안 바뀜 → 갱신 시 rootView 재생성
- [x] 세그먼트 `Picker`(전달 방식)는 NSSegmentedControl이라 케이스 identity가 그대로면
      제목을 다시 안 읽음 → `.id(loc.language)`로 재생성
- [x] `giftsheet.html`의 카드 문구 접근자를 `c()`로 두면 canvas `draw: function (c)`의
      파라미터에 가려짐 → `cardText()`로 명명

## 카드 디자인 5종 (완료)

- [x] `CardStyle` — classic · centered · ticket · poster · minimal.
      색상(`GiftTheme`) 5종과 직교하므로 25가지 조합. 스타일은 **배치 + 카드 자체의 그림**
      (모서리 반경 · 타입 스케일 · 광택 · 헤어라인 프레임 · 티켓 절취선) 둘 다를 정함
- [x] 배치 기본값을 스타일별로 분리 — `CardLayout.standard` → `CardLayout.defaults(for:)`.
      `CardLayout`이 자기 `style`을 들고 다니게 해서 "여기 기본값이 뭐냐 / 어디에 저장하냐"를
      호출자가 기억할 필요 없게 함 (한 스타일 배치가 다른 스타일로 새는 걸 막는 지점)
- [x] 드래그 배치를 스타일별로 저장 (`GiftWrap.cardLayout.<style>`).
      스타일 전환 시 현재 배치를 저장하고 새 스타일 것을 불러옴 → 옮겨 둔 작업이 안 날아감.
      스타일 없던 시절의 저장분은 classic으로 1회 이관
- [x] 편집기에 도식 썸네일 5개 피커. 실제 카드를 5개 렌더하면 QR·아이콘까지 1cm로 그리게 되므로
      막대 도식으로 — 고르는 대상(카드의 형태)만 이 크기에서 읽힘
- [x] 마지막에 고른 스타일을 기억 (`GiftWrap.cardStyle`)
- [x] payload에 `st` 키 → 받는 사람 페이지도 같은 스타일로 열림.
      드래그 배치(~260B)는 여전히 안 실림, 스타일은 한 단어라 실림
- [x] 앱 5종 · 선물 페이지 5종 스크린샷 확인, 콘솔 무오류

### 이 작업에서 고친 곳

- [x] centered가 실제로 가운데가 아니었음 — 제목/메시지 블록이 640·700 폭을 잡은 채
      좌측 고정이라 시각적 중심이 우측으로 밀림 → 박스를 (1000−폭)/2에 놓아 안쪽 정렬이
      카드 중심에 오도록
- [x] ticket에서 앱 이름 블록(640 폭)이 절취선 700을 넘어 QR과 충돌 →
      `titleMaxWidth`/`messageMaxWidth`를 스타일별로
- [x] poster 헤드라인 배율 3.4는 앱 이름(44pt)과 구분이 안 됨 → 5.0, 자간도 축소
- [x] 선물 페이지 티켓 노치가 4배 크게 나옴 — `.card`가 스스로 컨테이너라 자기 속성의
      `cqw`는 카드가 아닌 뷰포트 기준. `circle … cqw` → 요소 기준인 `ellipse 2.2% 3.492%`
      (자식/의사요소의 `cqw`는 정상 동작 — minimal 프레임은 그대로 둠)
- [x] `GiftCardView`가 다른 스타일용 배치를 받으면 그 스타일 좌표를 이 스타일 그림 위에 뿌림
      → 불일치 시 스타일 기본값으로 떨어지게. 일괄 탭 미리보기가 배치를 아예 안 넘기고
      있던 것도 같이 수정 (내보내기는 넘기고 있었음 — 미리보기가 결과와 달랐음)

### 남은 확인

- [ ] 스타일별 드래그 배치 유지를 실제 드래그로 확인 — osascript 보조 접근이 다시 거부되어
      GUI 자동화 불가. 스타일 전환·렌더는 `defaults write GiftWrap.cardStyle` + 재실행으로 확인함

## 웹앱 `web/studio.html` (완료)

- [x] macOS 앱과 같은 3탭 구성 — 카드 만들기 / 여러 장 만들기 / 보낸 기록
- [x] 앱 조회(iTunes Lookup, 스토어프론트 폴백) · 전달 방식 3종 · 코드 · 만료일
- [x] 디자인 5종 · 색상 5종 · 카드 언어 — 카드는 canvas로 그림.
      미리보기와 PNG 내보내기가 **한 렌더러의 두 배율**이라 배치한 대로 나감
- [x] 드래그 편집기 — 스냅·가이드선·모서리 리사이즈·방향키·+/−·겹침 경고,
      스타일별 배치 저장(localStorage)
- [x] 링크 3종(선물 페이지 · 리딤 · 전송 문구) · 이미지 복사 · PNG 저장 ·
      받는 화면 미리보기 · 보낸 기록(검색·사용됨·CSV)
- [x] 일괄 — `코드, 이름` 목록 → CSV + PNG 일괄 저장, 대장에 자동 기록
- [x] payload를 앱과 동일하게 생성 확인 (키 `b,c,d,e,f,g,i,k,lg,m,n,o,p,q,r,st,u,v`)
- [x] UI 언어 토글 + **카드 언어 기본값**을 앱·웹 양쪽에서 선택·기억
      (앱: `GiftWrap.cardLanguage`, 웹: `giftwrap.cardLang`)

### 이 작업에서 고친 곳

- [x] 포스터 · 센터의 메시지 상자가 코드 칩/QR과 겹침 — 상자 폭은 텍스트 길이와 무관하게
      `messageMaxWidth`만큼이라, 두 스타일에서 실제로 충돌.
      포스터 700→480, 센터 700→600(중앙 x 0.15→0.20). 앱·studio·gift 3곳 반영
- [x] `updateHint()`가 `render()` **앞**에서 호출돼 이전 스타일의 박스로 겹침을 판정 —
      센터의 충돌이 미니멀에 표시됨. `render()` 끝에서 한 번만 호출하도록
- [x] 스타일을 **방문만 해도** `saveLayout()`이 그때의 기본값을 저장 →
      이후 기본값 수정이 영영 도달 못 함. 기본값과 같으면 저장하지 않고 키를 지우도록 (앱·웹)
- [x] 한국어 조사가 안 맞음 — "메시지**과** 사람**가** 겹칩니다".
      받침 유무로 와/과·이/가·을/를을 고르는 헬퍼 추가 (앱 `Particle`, 웹 `waGwa`/`iGa`)
- [x] 웹 파일명이 한글을 다 날려 `-100-.png` — `\p{L}`로 바꿔 Swift와 동작 일치
- [x] 썸네일이 1px 막대로 찌그러짐 (`<span>`은 inline이라 width/height 무시) · 일괄 탭
      캔버스가 패널을 넘침 · 세그먼트 컨트롤이 폭 전체로 늘어남
- [x] 클립보드가 `file://`에서 항상 실패 — `execCommand` 폴백 추가

### 배포 (gh-pages)

- [x] `gh-pages` = `gift.html` + `studio.html` + `qr.js`. main의 `web/` 3개를 루트로 복사.
      **주의**: gh-pages에는 `.gitignore`가 없어 `git add -A`가 `.DS_Store`·xcuserstate까지
      쓸어담음 — 파일을 명시해서 add할 것
- [x] 라이브에서 조회·5종·링크 생성까지 실동작 확인

### Apple lookup API의 CORS 캐시 (해결)

- [x] 증상 — 배포본에서만 앱 조회가 `TypeError: Failed to fetch`. 로컬에서는 정상
- [x] 원인 — Apple이 `Access-Control-Allow-Origin`에 **요청한 오리진을 그대로 되돌려주는데**,
      CDN이 그 헤더까지 캐시하면서 `Vary: Origin`을 붙이지 않음.
      즉 그 URL을 처음 데운 사람이 "누가 읽을 수 있는지"를 정해 버림.
      실제로 `country=kr` 응답에 제 로컬 테스트가 남긴
      `access-control-allow-origin: http://127.0.0.1:8931`가 박혀 있었음
- [x] 해결 — 요청마다 `cb=<nonce>`를 붙여 각자 캐시 항목과 헤더를 갖게 함.
      조회는 버튼을 눌러야 일어나므로 포기하는 캐시는 원래 거의 없음.
      `giftsheet.html`도 같은 노출이라 함께 수정
- 진단 교훈 두 가지:
  (1) 브라우저 콘솔에서 실행한 `fetch`는 확장 권한 컨텍스트라 **CORS를 우회**함 —
      판정 근거로 쓸 수 없고, curl에 `Origin` 헤더를 실어 확인해야 함
  (2) GitHub Pages HTML은 브라우저가 캐시함. 배포 검증은 쿼리스트링을 붙여 우회할 것
      (안 그러면 stale 파일을 두고 "고쳐도 안 된다"를 반복하게 됨)

### 남은 확인

- [ ] 앱에서 포스터·센터를 **메시지가 있는 상태로** 눈으로 확인 — osascript 보조 접근이
      거부되어 앱에 글자를 못 넣음. 같은 좌표를 쓰는 studio에서는 내용 채워 확인함

## 카드 겹침 · 받는 카드 글자 크기 (완료)

- [x] 근본 원인 — 기본 배치를 **12자 테스트 코드**로 예산했는데 실제 오퍼 코드는 **18자**.
      칩이 303pt로 불어나 QR을 침범하고 티켓에서는 카드 밖으로 나감.
      내용이 거의 없는 sparse 프로필에서도 5종 중 4종이 깨졌음
- [x] 숫자만 다시 맞추면 또 깨지므로 **자동 fit 패스**를 넣음 — 측정 후 겹치는 쪽을 4%씩
      줄여 재측정, 아무것도 겹치지 않고 카드 밖으로 나가지 않을 때까지.
      아트워크(QR·아이콘)는 버티고 텍스트가 먼저 양보, 텍스트가 한계(0.5)에 닿으면
      마지막 수단으로 아트워크도 0.75까지 양보. 매 그리기마다 새로 계산하고 저장하지 않음
      → 맞는 카드는 작성한 그대로 나감
- [x] 단순 비겹침이 아니라 **간격(8/1000)**을 요구 — 칩 테두리가 QR에 딱 붙으면 실수처럼 보임
- [x] 18자 기준으로 code 위치 재예산 (classic/poster 0.48, minimal 0.49, centered 0.348,
      ticket 0.721 + 0.85배). 앱·studio·gift 3곳
- [x] 받는 카드 글자 — 폰에서 카드 폭 ~357px, 코드가 7.6px였음.
      `.stage` 420→520px, 카드 타입 배율 ×1.32(`CARD_TYPE_BOOST`), QR 10.4→13cqw.
      **결과: 코드 7.6→11.1px, QR 36→77px.** fit 패스가 있어서 키우는 게 안전해짐
- [x] 검증 — studio 5종×3프로필, 선물 페이지 5종(360px 폰 폭·내용 가득) 전부 무충돌

### 이 작업에서 고친 곳

- [x] fit 패스가 `getBoundingClientRect()`로 재는데, `reveal` 애니메이션이 `scale(.94)`로
      도는 동안 실행되면 **변형된 크기**(488px)로 수렴해 버림. 애니메이션은 레이아웃 박스를
      안 바꾸니 ResizeObserver도 다시 안 부름 → 영영 어긋난 채로 남음.
      변형에 영향받지 않는 `offsetLeft/offsetWidth`로 교체
- [x] 축소만으로는 못 고치는 경우 — 블록 원점이 고정이라 아래 블록을 아무리 줄여도
      위 블록 밑에서 못 빠져나옴(티켓 code×qr). 선호 양보자가 한계면 상대에게 넘기도록,
      그리고 티켓 code y를 QR 아래로 내림(0.5238→0.59)

### 앱(PNG) 쪽 범위

- 앱에는 전체 fit 패스가 아직 없음. SwiftUI는 그리기 전에 블록 크기를 알 수 없어
  같은 반복 측정이 어려움. 대신 **코드 칩만** 폭을 계산해 넘치면 줄이도록 함
  (모노스페이스는 글자수×0.6em으로 폭이 계산 가능한 유일한 요소).
  긴 메시지·긴 앱 이름으로 인한 세로 겹침은 여전히 편집기 경고로만 알림

## 결정 대기

- [ ] 실물 제작 방식 확정 — (A) 집프린터 N-up 재단 / (B) 풀 링크 + 동일 카드 인쇄 / (C) 인쇄소 카드 + 라벨 스티커
- [ ] (B) 선택 시 Cloudflare Worker + KV 디스펜서 작성 — 카드 전부 동일 디자인, 재인쇄 가능, 사용량 추적

## 나중에

- [ ] GiftWrap 앱 본체에 인쇄 시트 내보내기 통합 (지금은 코드별 PNG만 나옴)
- [ ] 코드 유효기간·소진 현황을 대장에 반영
